/**
 * ZoneCanvas - 위험 구역을 그리는 투명 캔버스 컴포넌트 (스켈레톤)
 *
 * TODO: 프론트엔드 담당자가 실제 UI 라이브러리에 맞게 구현
 * - 영상 위에 투명 오버레이로 올라감
 * - 사용자가 터치/클릭으로 다각형 꼭짓점을 찍을 수 있음
 * - 서버에서 받아온 기존 구역을 화면에 표시
 */

// placeholder - 프레임워크 선택 후 구현
export default function ZoneCanvas() {
  // TODO: 구현
}
